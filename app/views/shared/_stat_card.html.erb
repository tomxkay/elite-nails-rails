<%# Stat card for metrics
  Locals:
    - value: primary metric (string/number, optional if icon_name provided)
    - label: supporting text
    - icon_name: icon helper name (optional)
    - icon_variant: :outline or :solid (default :outline)
    - icon_classes: custom icon classes
%>
<% icon_variant = local_assigns.fetch(:icon_variant, :outline) %>
<% icon_classes = local_assigns.fetch(:icon_classes, "w-10 h-10 text-terracotta-500 mx-auto mb-2") %>
<% icon_name = local_assigns[:icon_name] %>
<% value = local_assigns[:value] %>
<% label = local_assigns[:label] %>

<div class="card-surface-warm rounded-2xl p-6 text-center">
  <% if icon_name.present? %>
    <div class="flex justify-center mb-3">
      <%= icon icon_name, variant: icon_variant, classes: icon_classes %>
    </div>
  <% end %>

  <% if value.present? %>
    <div class="font-serif text-4xl text-terracotta-600 mb-2 drop-shadow-sm"><%= value %></div>
  <% end %>

  <div class="text-warm-gray-700 text-sm font-semibold tracking-wide uppercase"><%= label %></div>
</div>
